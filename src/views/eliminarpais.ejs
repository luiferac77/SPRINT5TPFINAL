<main class="flex-grow container mx-auto p-6">
    <section>
            <div><h2 class="text-3xl font-bold text-gray-900 p-4">¿Deseas eliminar el país?</h2></div>
            <form action="/api/paises/eliminar/<%= pais._id %>" method="POST" class="bg-white text-gray-900 p-6 rounded-lg shadow-md space-y-6">
            <div>
                <p><strong>Nombre del país:</strong> <%= pais.name.common %></p>
                <p><strong>Nombre oficial del país:</strong> <%= pais.name.official %></p>
            </div>
            <div class="flex justify-end space-x-2 mt-4">
            <a href="/api/paises/dashboard">
                <button type="button" class="flex items-center bg-blue-500 text-white hover:bg-blue-600 transition gap-2 px-4 py-2 m-2 text-sm font-semibold rounded-lg shadow-sm">Cancelar</button>
            </a>
            <button type="submit" class="flex items-center bg-red-800 text-white hover:bg-red-700 transition gap-2 px-4 py-2 m-2 text-sm font-semibold rounded-lg shadow-sm"> Aceptar</button>
            </div>
        </form>
    </section>
</main>